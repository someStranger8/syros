syros_sources = files('disk.c', 'kmain.c', 'memory.c', 'serial.c',)

# nasm = find_program('nasm')
# nasm_gen = generator(
#     nasm,
#     output: '@BASENAME@.o',
#     arguments: ['-f', 'elf64', '-g', '-F', 'dwarf', '@INPUT@', '-o', '@OUTPUT@'],
# )
